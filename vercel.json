{
  "buildCommand": "pnpm install && pnpm -w run deploy:build",
  "functions": {
    "api/index.js": {
      "includeFiles": "artifacts/api-server/dist/**",
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
